import { makeStyles } from '@material-ui/core/styles';

export default makeStyles (() => ({
    card: {
        borderRadius: '10px'
    },
    button: {
        borderRadius: '15px',
        marginTop: '5px',
        marginLeft: '45px'
    }
}))
