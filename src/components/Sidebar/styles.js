import { makeStyles } from '@material-ui/core/styles';

export default makeStyles (() => ({
    container: {
        borderRadius: '15px'
    },
    card: {
        borderRadius: '10px',
        justifyContent: 'center'
    },
    button: {
        borderRadius: '15px',
        marginTop: '5px',
        marginBottom: '5px',
        alignItems: 'center',
        alignContent: 'center',
        justifyContent: 'center'
    }
}))
